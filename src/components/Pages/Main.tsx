import React, { useState } from 'react';
import ExpansionCustomPanel from './ExpansionCustomPanel'

export default function Main() {
  // const [steps, setSteps] = useState(0);

  // function increment() {
  //   setSteps(prevState => prevState + 1);
  // }

  return (
    <React.Fragment>
      <ExpansionCustomPanel />
    </React.Fragment>
  );
}